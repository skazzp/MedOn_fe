import { LoadingOutlined } from '@ant-design/icons';
import styled from 'styled-components';

export const LoadingStyledOutlined = styled(LoadingOutlined)`
  color: ${({ theme }) => theme.colors.white};
`;
