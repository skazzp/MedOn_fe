import styled from "styled-components";
import { theme } from "styles/theme";

export const Wrapper = styled.div`
  display: flex;
  margin: 4rem;
  border-radius: 2px;
	box-shadow: 14px 18px 26px 12px rgba(0,0,0,0.1);
	background: ${theme.colors.white};
  height: 899px;
`